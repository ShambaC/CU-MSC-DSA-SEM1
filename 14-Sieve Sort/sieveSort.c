#include<stdio.h>
#include<stdlib.h>

struct subArr {
    
};

/**
 * method for sieve sort
 * @param arr   the input array
 * @param size  size of the input array
 * @param dir   direction of sieveing
 * 
 * dir = 0 => left to right
 * dir = 1 => right to left
*/
void sort(int arr[], int size, int dir)
{
    if(dir == 0)
    {
        int reference = arr[0];
        for(int i = 1; i < size; i++)
        {

        }
    }
}